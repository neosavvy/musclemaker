import React, { Component } from 'react';
import BaseButton from './base_button.component';

export default class PrimaryBaseButton extends BaseButton {
    
    constructor(props) {
        super(props);
    }
    
}